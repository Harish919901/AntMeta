services:
  - type: web
    name: antmeta-magic-link
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: ADMIN_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
